package com.pomo.ui.components

// The radial 24-hour spoke clock was removed. The hour-of-day chart now lives in
// HourBarChart24.kt as a standard horizontal bar chart. This file is intentionally
// empty to avoid breaking any stale imports until they're updated; nothing references
// `RhythmClock` after the Stats redesign.
